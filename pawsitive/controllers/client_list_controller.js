var user = require('./../server/config/config');
var express = require('express');


exports.client_list = function(req, res) {
	res.send("controller - n/a");
	//pull db or local storage for client JSON
	//convert json to html doc frag and inject
	//store doc frag in local storage with timestamp
	
};
